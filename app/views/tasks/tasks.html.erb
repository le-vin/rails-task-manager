<h1>Tasks</h1>
<% @tasks.each do |task| %>
  <input type="checkbox" name="complete" value="boolean" <% if task[:completed] == true  %> <%= "checked" %> <% end %>><%= link_to task[:title], task_path(task[:id]) %><br>
<% end %>
<%= link_to "Add a task", new_path %>
